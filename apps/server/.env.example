# Copy to apps/server/.env (gitignored) only if you want accounts to survive a restart.
# Without DATABASE_URL the server keeps accounts in memory, which is fine for local work.
# Any Postgres works; the owner uses a Neon branch (see docs/deploy.md).
# DATABASE_URL=postgresql://user:password@host/db?sslmode=verify-full

# PORT=8033
